server:
  port: 8081

ssrpc:
  zkServer: localhost:2181
  zkRoot: ssrpc

app:
  id: app1
  namespace: public
  env: dev
  metas: "{dc: 'bj', gray: 'false', unit: 'B001'}"

logging:
  level:
    root: error
    com.stephenshen.ssrpc: info