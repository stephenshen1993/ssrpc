server:
  port: 8084

ssrpc:
  zkServer: localhost:2182
  zkRoot: ssrpc

app:
  id: app1
  namespace: public
  env: dev
  metas: "{dc: 'bj', gray: 'false', unit: 'B001'}"

logging:
  level:
    root: error
    com.stephenshen.ssrpc: debug